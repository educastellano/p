'use strict';


P.View.onModelChange = function onModelChange (e, args) {
    var el,
        text,
        value,
        i;
    debugger; // TODO fix and re-think this
//            for (i=0; i<this.el.length; i++) {
//                el = $(this.el[i]);
//                text = el.data('text');
//                if (text && text === args.attr) {
//                    if (this.viewModel && this.viewModel[args.attr]) {
//                        value = this.viewModel[args.attr];
//                    }
//                    else {
//                        value = args.value;
//                    }
//                    el.text(value);
//                }
//            }
};