'use strict';

P.persistMongo = function (method, model, options) {
    var returnData;

    // perform operation
    if (method === 'create') {

    }
    else if (method === 'update') {

    }
    else if (method === 'delete') {

    }
    else if (method === 'read') {

    }
    else {

    }

    if (options.success) {
        options.success(returnData);
    }
};
